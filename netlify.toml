[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# This ensures React Router works correctly when you refresh the page
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
